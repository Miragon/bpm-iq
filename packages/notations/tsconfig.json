{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    // content.ts is the node-only subpath (fs) — index/extract/derive stay
    // browser-safe; node types here only affect the typecheck, not the bundle
    "types": ["node"]
  },
  "include": ["*.ts", "test"]
}
