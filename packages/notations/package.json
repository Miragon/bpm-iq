{
  "name": "@bpmiq/notations",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "The notation registry: one descriptor per modeling notation (extensions, media kind, editor language). Adding a notation to the platform starts here.",
  "engines": {
    "node": ">=23.6"
  },
  "exports": {
    ".": "./index.ts",
    "./extract": "./extract.ts",
    "./derive": "./derive.ts",
    "./content": "./content.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "node --test test/*.test.ts"
  },
  "dependencies": {
    "fast-xml-parser": "5.10.1",
    "yaml": "2.9.0"
  },
  "devDependencies": {
    "@types/node": "26.1.1"
  }
}
