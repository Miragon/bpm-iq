{
  "name": "@bpmiq/decisions",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "DMN decision simulation, analysis and test running — ISOMORPHIC: the same FEEL engine and the same answers in Node (MCP, CI) and in the browser (the SPA's DMN editor, the MCP-App widget).",
  "engines": {
    "node": ">=23.6"
  },
  "//": "The exported entries are browser-safe (CI-enforced: `pnpm arch`, rule decisions-lib-stays-isomorphic). ./cli is the ONLY Node entry and is deliberately not exported.",
  "exports": {
    ".": "./index.ts",
    "./tests": "./tests.ts"
  },
  "scripts": {
    "check": "node cli.ts",
    "typecheck": "tsc --noEmit",
    "test": "node --test test/*.test.ts"
  },
  "dependencies": {
    "@bpmiq/notations": "workspace:*",
    "@emaarco/dmn-js-simulation": "0.2.0",
    "feelin": "7.0.1",
    "inferno": "5.6.3",
    "yaml": "2.9.0"
  },
  "devDependencies": {
    "@types/node": "26.1.2"
  }
}
