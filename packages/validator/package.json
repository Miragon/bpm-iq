{
  "name": "@bpmiq/validator",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Platform-owned validator for BPM content repos — schema, link integrity, BPMN/DMN structure, governance, export freshness. Runs against any checkout via --root; never executes content-repo code.",
  "engines": {
    "node": ">=23.6"
  },
  "bin": {
    "bpmiq-validate": "src/validate.ts"
  },
  "exports": {
    ".": "./src/validate.ts"
  },
  "scripts": {
    "validate": "node src/validate.ts",
    "typecheck": "tsc --noEmit",
    "test": "node --test test/validate.test.ts"
  },
  "dependencies": {
    "@bpmiq/notations": "workspace:*",
    "ajv": "^8.17.0",
    "ajv-formats": "^3.0.0",
    "fast-xml-parser": "^4.5.0",
    "yaml": "^2.6.0"
  }
}
