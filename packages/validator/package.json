{
  "name": "@bpmiq/validator",
  "version": "3.7.0",
  "type": "module",
  "description": "Platform-owned validator for BPM content repos — bpmiq.yml discovery + BPMN structure and BPMNDI coverage. Runs against any checkout via --root; never executes content-repo code.",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Miragon/bpm-iq.git",
    "directory": "packages/validator"
  },
  "keywords": [
    "bpm",
    "bpmn",
    "validator",
    "cli"
  ],
  "engines": {
    "node": ">=22"
  },
  "bin": {
    "bpmiq-validate": "src/cli.ts"
  },
  "exports": {
    ".": "./src/validate.ts"
  },
  "files": [
    "dist"
  ],
  "publishConfig": {
    "access": "public",
    "exports": {
      ".": "./dist/validate.js"
    },
    "bin": {
      "bpmiq-validate": "dist/cli.js"
    }
  },
  "scripts": {
    "validate": "node src/cli.ts",
    "build": "tsdown",
    "typecheck": "tsc --noEmit",
    "test": "node --test test/*.test.ts"
  },
  "//": "fast-xml-parser stays a RUNTIME dependency although src/ no longer imports it directly: tsdown inlines @bpmiq/notations (whose extract.ts imports it) but keeps npm deps external, so the published dist/ resolves it from here. Removing it breaks the npm artifact at import time while the workspace keeps working.",
  "dependencies": {
    "fast-xml-parser": "5.10.1",
    "yaml": "2.9.0"
  },
  "devDependencies": {
    "@bpmiq/notations": "workspace:*",
    "tsdown": "0.22.14"
  }
}
