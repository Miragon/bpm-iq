{
  "name": "@bpmiq/http-kit",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Shared node:http primitives for the bpmiq backends — zero dependencies (copies cleanly into the dependency-free control-plane image)",
  "engines": {
    "node": ">=23.6"
  },
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "node --test test/*.ts"
  },
  "devDependencies": {
    "@types/node": "^26.1.1",
    "typescript": "^5.9.3"
  }
}
