/**
 * The blank-model templates live in @bpmiq/notations/templates — the template
 * capability of the notation registry (epic #118 step 3). Re-exported here so
 * the existing live-host import paths stay stable.
 */
export { escapeXml, newBpmnXml, xmlProcessId } from "@bpmiq/notations/templates";
